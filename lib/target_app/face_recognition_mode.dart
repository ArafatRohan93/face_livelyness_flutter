/// Face recognition mode defines if the user image will take in live or manually by
/// user capture
enum FaceRecognitionMode { live, manual }

/// View Mode wll decide weather user face will recognize or get predicted data
enum ViewMode { recognition, capture }
